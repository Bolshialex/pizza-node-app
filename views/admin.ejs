<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Order Summary</h1>
    <ul>
      <% for( let i = 0; i < pizzaOrders.length; i++ ) { %>
      <h1><%= pizzaOrders[i].fname %> <%= pizzaOrders[i].lname %></h1>

      <li><%= pizzaOrders[i].email %></li>
      <li><%= pizzaOrders[i].method %></li>
      <li><%= pizzaOrders[i].toppings %></li>
      <li><%= pizzaOrders[i].size %></li>
      <hr />
      <% } %>
    </ul>
  </body>
</html>
